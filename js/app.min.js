!(function r(n, i, s) {
  function l(t, e) {
    if (!i[t]) {
      if (!n[t]) {
        const a = typeof require === 'function' && require;
        if (!e && a) return a(t, !0);
        if (d) return d(t, !0);
        const o = new Error("Cannot find module '" + t + "'");
        throw ((o.code = 'MODULE_NOT_FOUND'), o);
      }
      const c = (i[t] = {
        exports: {},
      });
      n[t][0].call(
        c.exports,
        function (e) {
          return l(n[t][1][e] || e);
        },
        c,
        c.exports,
        r,
        n,
        i,
        s
      );
    }
    return i[t].exports;
  }
  for (
    var d = typeof require === 'function' && require, e = 0;
    e < s.length;
    e++
  )
    l(s[e]);
  return l;
})(
  {
    1: [
      function (e, t, a) {
        const l = document.querySelectorAll('.anim-item');
        if (l.length > 0) {
          const o = function (e) {
            for (let t = 0; t < l.length; t++) {
              const a = l[t];
              const o = a.offsetHeight;
              const c = a.offsetWidth;
              const r = d(a).top;
              const n = d(a).left;
              let i = window.innerHeight - o / 3;
              let s = window.innerWidth - c / 3;
              o > window.innerHeight
                ? (i = window.innerHeight - window.innerHeight / 3)
                : c > window.innerWidth &&
                  (s = window.innerWidth - window.innerWidth / 3),
                pageYOffset > r - i &&
                pageYOffset < r + o &&
                pageXOffset > n - s &&
                pageXOffset < n + c
                  ? a.classList.add('active')
                  : a.classList.contains('anim-item--no-hide') ||
                    a.classList.remove('active');
            }
          };
          var d = function (e) {
            const t = e.getBoundingClientRect();
            const a = window.pageXOffset || document.documentElement.scrollLeft;
            const o = window.pageYOffset || document.documentElement.scrollTop;
            return {
              top: t.top + o,
              left: t.left + a,
            };
          };
          window.addEventListener('scroll', o),
            setTimeout(function () {
              o();
            }, 300);
        }
        $(window).scroll(function () {
          $(this).scrollTop() >= 400 && $('.consulting').slideUp(600);
          const e = $(window).height();
          const t = $(window).scrollTop();
          if ($('.about-scope').length) {
            const a = $('.about-scope').height();
            const o = $('.about-scope').offset().top + a - e;
            const c = $('.about-scope').offset().top;
            if (o < t && t < c) var r = `${((t - o) / (c - o)) * 100}%`;
            $('.about-scope').css('background-position-y', r);
          }
          $(this).scrollTop() >= 1200
            ? $('.to-top').addClass('active')
            : $('.to-top').removeClass('active');
        });
        const c = document.querySelector('.history');
        const r = document.querySelector('.history-scroll');
        if (c) {
          const n = function (e, t) {
            const a = window.pageYOffset + e.getBoundingClientRect().top;
            const o = window.pageYOffset + e.getBoundingClientRect().bottom;
            const c = t.getBoundingClientRect().width;
            const r = t.getBoundingClientRect().height;
            const n = window.pageYOffset;
            const i =
              window.pageYOffset + document.documentElement.clientHeight;
            $(e).css('height', `${c}px`);
            const s =
              (100 *
                (($(window).scrollTop() - a) / (c - r)) *
                (c - document.documentElement.clientWidth)) /
              c;
            a <= n
              ? ($(t).removeClass('start'),
                $(t).addClass('fixed'),
                $(t).css({
                  transform: `translateX(-${s}%)`,
                  '-webkit-transform': `translateX(-${s}%)`,
                }))
              : ($(t).addClass('start'), $(t).removeClass('fixed')),
              o < i ? $(t).addClass('end') : $(t).removeClass('end');
          };
          window.addEventListener('scroll', function () {
            n(c, r);
          }),
            setTimeout(function () {
              n(c, r);
            }, 300);
        }

        const cssValuesVis = {
          opacity: '1',
          height: 'auto',
        };
        const cssValuesHid = {
          opacity: '0',
          height: '0',
        };

        $('.page-id-2 .main-screen__image-text span').css(cssValuesHid);
        $('.page-id-2 .main-screen__image-text span:nth-of-type(1)').css(
          cssValuesVis
        );

        if (window.innerWidth >= 770) {
          showText();

          function showText() {
            const textItem = $('.page-id-2 .main-screen__image-text span');
            textItem.css(cssValuesHid);
            $('.page-id-2 .main-screen__image-text span:nth-of-type(1)').css(
              cssValuesVis
            );

            setTimeout(function () {
              textItem.css(cssValuesHid);
              $('.page-id-2 .main-screen__image-text span:nth-of-type(2)').css(
                cssValuesVis
              );
            }, 3000);
            setTimeout(function () {
              textItem.css(cssValuesHid);
              $('.page-id-2 .main-screen__image-text span:nth-of-type(3)').css(
                cssValuesVis
              );
            }, 6000);
          }
          setInterval(function () {
            showText();
          }, 9000);
        } else {
          showText();

          function showText() {
            const textItem = $('.page-id-2 .main-screen__image-text span');
            textItem.css(cssValuesHid);
            $('.page-id-2 .main-screen__image-text span:nth-of-type(1)').css(
              cssValuesVis
            );
            console.log('change');
            setTimeout(function () {
              textItem.css(cssValuesHid);
              $('.page-id-2 .main-screen__image-text span:nth-of-type(2)').css(
                cssValuesVis
              );
            }, 4000);
            setTimeout(function () {
              textItem.css(cssValuesHid);
              $('.page-id-2 .main-screen__image-text span:nth-of-type(3)').css(
                cssValuesVis
              );
            }, 8000);
          }
          setInterval(function () {
            showText();
          }, 12000);
        }

        $('.main-screen__text-foreign span').css(cssValuesHid);
        $('.main-screen__text-foreign span:nth-of-type(1)').css(cssValuesVis);

        showTextForeign();

        function showTextForeign() {
          const textItem = $('.main-screen__text-foreign span');
          textItem.css(cssValuesHid);
          $('.main-screen__text-foreign span:nth-of-type(1)').css(cssValuesVis);

          setTimeout(function () {
            textItem.css(cssValuesHid);
            $('.main-screen__text-foreign span:nth-of-type(2)').css(
              cssValuesVis
            );
          }, 5000);
        }
        setInterval(function () {
          showTextForeign();
        }, 9000);

        $('a[href^="#"]').click(function () {
          const e = $(this).attr('href').replace(/#/, '');
          if (e && $(`a[name=${e}]`).length) {
            const t = $(`a[name=${e}]`).offset().top;
            return (
              $('html, body').animate(
                {
                  scrollTop: t,
                },
                1100
              ),
              !1
            );
          }
        }),
          $('.header__toggle').click(function () {
            $('body').addClass('no-scroll'),
              $('.header__menu').addClass('active');
          }),
          $('.header__menu-close').click(function () {
            $('body').removeClass('no-scroll'),
              $('.header__menu').removeClass('active');
          }),
          $('.form-request__open').click(function () {
            $('body').addClass('no-scroll'),
              $('.form__popup--request').addClass('active');
          }),
          $('.form__close').click(function () {
            $('body').removeClass('no-scroll'),
              $('.form__popup').removeClass('active');
          }),
          // $(".header__menu-link--cases").click(function() {
          //     $("body").addClass("no-scroll"), $(".portfolio-popup").addClass("active")
          // }),
          $('.portfolio-popup__close').click(function () {
            $('.portfolio-popup').removeClass('active'),
              $('.header__menu').hasClass('active') ||
                $('body').removeClass('no-scroll');
          }),
          $('.header__menu-link--servicessss').click(function () {
            $('body').addClass('no-scroll'),
              $('.services-popup').addClass('active');
          }),
          // $(".header__menu-link--carrer").click(function() {
          //     $("body").addClass("no-scroll"), $(".career-popup").addClass("active")
          // }),
          $('.services-popup__close').click(function () {
            $('.services-popup').removeClass('active'),
              $('.career-popup').removeClass('active'),
              $('.header__menu').hasClass('active') ||
                $('body').removeClass('no-scroll');
          }),
          $(
            '.career-vacancy__item:nth-of-type(odd) .career-vacancy__item-icon'
          ).click(function () {
            const t = this;
            $('.career-vacancy__item-icon').removeClass('active'),
              $(this).addClass('active'),
              $('.career-vacancy__wrapper').removeClass('active-right'),
              $('.career-vacancy__wrapper').addClass('active-left'),
              $('.career-vacancy__popup').slideUp(600),
              $(this)
                .closest('.career-vacancy__list')
                .prev('.career-vacancy__col')
                .children('.career-vacancy__popup')
                .slideDown(600),
              setTimeout(function () {
                const e = $(t)
                  .closest('.career-vacancy__list')
                  .prev('.career-vacancy__col')
                  .children('.career-vacancy__popup')
                  .offset().top;
                $('html, body').animate(
                  {
                    scrollTop: e - 40,
                  },
                  600
                );
              }, 600);
          }),
          $(
            '.career-vacancy__item:nth-of-type(even) .career-vacancy__item-icon'
          ).click(function () {
            const t = this;
            $('.career-vacancy__item-icon').removeClass('active'),
              $(this).addClass('active'),
              $('.career-vacancy__wrapper').removeClass('active-left'),
              $('.career-vacancy__wrapper').addClass('active-right'),
              $('.career-vacancy__popup').slideUp(600),
              $(this)
                .closest('.career-vacancy__list')
                .next('.career-vacancy__col')
                .children('.career-vacancy__popup')
                .slideDown(600),
              setTimeout(function () {
                const e = $(t)
                  .closest('.career-vacancy__list')
                  .next('.career-vacancy__col')
                  .children('.career-vacancy__popup')
                  .offset().top;
                $('html, body').animate(
                  {
                    scrollTop: e - 40,
                  },
                  600
                );
              }, 600);
          }),
          $('.career-vacancy__popup-close').click(function () {
            $(this).closest('.career-vacancy__popup').slideUp(600),
              $('.career-vacancy__item-icon').removeClass('active'),
              $('.career-vacancy__wrapper').removeClass('active-left'),
              $('.career-vacancy__wrapper').removeClass('active-right');
          }),
          $('.form__select').each(function () {
            $(this).select2({
              placeholder: $(this).attr('placeholder'),
              width: '100%',
              containerCssClass: 'form__select-container',
              dropdownCssClass: 'form__select-popup',
              minimumResultsForSearch: 8,
            });
          });
        const p = document.querySelectorAll('.marquee-item');
        if (p.length > 0) {
          !(function () {
            for (
              let e = function (e) {
                  const t = p[e];
                  const a = t.children[0];
                  const o = a.offsetWidth;
                  const c = 1e3 * t.getAttribute('data-marque-speed');
                  const r = a.cloneNode(!0);
                  t.appendChild(r);
                  let n = performance.now();
                  let i = void 0;
                  let s = void 0;
                  requestAnimationFrame(function e(t) {
                    (i = (t - n) / c) > 1 && ((i %= 1), (n = t)),
                      (s = o * i),
                      (a.style.transform = 'translateX(-'.concat(s, 'px)')),
                      (r.style.transform = 'translateX(-'.concat(s, 'px)')),
                      requestAnimationFrame(e);
                  });
                },
                t = 0;
              t < p.length;
              t++
            )
              e(t);
          })();
        }
        const f = document.querySelectorAll('.marquee-item-rev');
        if (f.length > 0) {
          !(function () {
            for (
              let e = function (e) {
                  const t = f[e];
                  const a = t.children[0];
                  const o = a.offsetWidth;
                  const c = 1e3 * t.getAttribute('data-marque-speed');
                  const r = a.cloneNode(!0);
                  t.appendChild(r);
                  let n = performance.now();
                  let i = void 0;
                  let s = void 0;
                  requestAnimationFrame(function e(t) {
                    (i = (t - n) / c) > 1 && ((i %= 1), (n = t)),
                      (s = o * i),
                      (a.style.transform = 'translateX('.concat(s, 'px)')),
                      (r.style.transform = 'translateX('.concat(s, 'px)')),
                      requestAnimationFrame(e);
                  });
                },
                t = 0;
              t < f.length;
              t++
            )
              e(t);
          })();
        }
      },
      {},
    ],
  },
  {},
  [1]
);
// # sourceMappingURL=app.min.js.map
